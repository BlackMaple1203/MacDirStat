import SwiftUI

@main
struct MacDirStatApp: App {
    var body: some Scene {
        WindowGroup("MacDirStat") {
            Text("Loading…")
        }
        .defaultSize(width: 1200, height: 800)
    }
}
